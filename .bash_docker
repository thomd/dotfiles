# vim:ft=sh

# awscli (https://github.com/thomd/dockerfiles/tree/master/awscli)
#
aws() {
  docker run -it --rm \
    -v "${HOME}/.aws:/root/.aws" \
    --log-driver none \
    --name aws \
    thomd/awscli "$@"
}
