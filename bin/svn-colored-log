#!/usr/bin/env ruby

`svn log -v #{ARGV.join(' ')}`.each do |line|
  puts( if line =~ /^r\d+\s\|(.*)$/
          "\033[31m#{$&}\033[0m" 
        elsif line =~ /^-(.*)$/
          "\033[32m#{$&}\033[0m" 
        else
          line
        end
      )
end