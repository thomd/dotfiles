#!/usr/bin/env ruby

`svn diff #{ARGV.join(' ')}`.each do |line|
  puts( if line =~ /^\+(.*)$/
          "\033[32m#{$&}\033[0m" 
        elsif line =~ /^-(.*)$/
          "\033[31m#{$&}\033[0m" 
        else
          line
        end
      )
end